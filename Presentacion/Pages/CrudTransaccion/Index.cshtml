@page
@model Presentacion.Pages.CrudTransaccion.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Transaccion[0].NumeroFactura)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Transaccion[0].Valor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Transaccion[0].Sucursal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Transaccion[0].Mes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Transaccion[0].EsCompra)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Transaccion) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumeroFactura)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Valor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sucursal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EsCompra)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
